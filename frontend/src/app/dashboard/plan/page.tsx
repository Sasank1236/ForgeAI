import { PlannerInterface } from "./PlannerInterface";

export const metadata = {
  title: "AI Task Planner — ForgeAI",
  description: "AI-driven task decomposition and targeted code edit suggestions.",
};

export default function PlannerPage() {
  return <PlannerInterface />;
}
